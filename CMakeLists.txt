CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

SET(CMAKE_INSTALL_PREFIX /usr)
SET(PREFIX ${CMAKE_INSTALL_PREFIX})

MESSAGE("PROFILE_TARGET = ${PROFILE_TARGET}")

ADD_SUBDIRECTORY(certificates/${PROFILE_TARGET})
ADD_SUBDIRECTORY(policy/${PROFILE_TARGET})
